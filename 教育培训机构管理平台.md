## 项目主要需求

### 项目概述

构建一套面向教育培训机构的综合管理平台，覆盖招生获客、学员管理、课程排班、财务收费、教师绩效等核心业务场景。系统帮助培训机构实现从线索跟进到学员缴费上课的全流程数字化管理，提升运营效率和管理水平。

### 功能需求

1. **CRM 招生管理**：录入和管理潜在学员线索（姓名、联系方式、意向课程、来源渠道），支持线索分配给课程顾问跟进，记录每次跟进的沟通内容和结果，线索状态自动流转（待联系→已试听→已报名→已流失）
2. **学员管理**：学员报名后自动从线索转为正式学员，管理学员信息、就读课程、学习进度和历史缴费记录，支持学员标签管理（如"续费提醒"、"优秀学员"）方便精细化运营
3. **课程与排课管理**：创建课程产品（一对一、小班课、大班课），设置课时单价和总课时数，教师排课后系统自动检测教室和教师时间冲突，学员端可查看自己的课表
4. **考勤管理**：教师通过系统进行学员点名签到，支持按课时扣费（消耗课时），学员家长收到上课通知和考勤结果推送，考勤记录自动关联课消统计
5. **财务管理**：学员缴费支持多种收款方式（现金、微信、支付宝、银行转账），自动生成收费收据，提供收支明细账和财务报表（日报、月报、年报），支持退费流程管理
6. **教师管理与绩效**：维护教师信息（姓名、资质、任教科目、课时费标准），系统根据教师实际上课考勤自动统计月度课时量，按课时费标准计算绩效工资
7. **数据看板**：机构运营数据总览——本月新增线索数、试听转化率、报名人数、课消金额、续费率等关键指标，支持图表和趋势对比展示

### 技术要求

- 前端：React + TypeScript，使用 Ant Design 组件库，Vite 构建工具
- 后端：Go 语言 + Gin 框架
- 数据库：MySQL 8.0，使用 GORM 做数据持久化
- 缓存：Redis 用于数据缓存和分布式锁
- 认证方式：JWT + Casbin 权限管理
- 图表：前端使用 ECharts 绘制运营数据看板
- 文件生成：Go 标准库生成 Excel 报表

## 代码存放规则

这是前后端分离项目。

前端代码放在 `frontend/`：

页面，组件，路由,状态管理,API 请求,类型定义,样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：

数据库脚本放在 `database/`。

## 部署要求

项目必须支持 Docker Compose 一键部署。

需要提供以下文件：

- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- `frontend/Dockerfile`：前端项目构建与运行镜像（多阶段构建，先构建再用 Nginx 托管）
- `backend/Dockerfile`：后端项目构建与运行镜像
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明

docker-compose.yml 要求：

- 所有服务使用命名容器，便于管理
- 数据库数据使用命名卷持久化
- 前端通过 Nginx 反向代理后端 API
- 服务间通过 Docker 内部网络通信
- 数据库服务配置 healthcheck，后端服务依赖数据库健康检查通过后再启动
- 暴露端口（避免与其他项目冲突）：前端 8005，后端 3005，MySQL 3402，Redis 6405
- 支持通过 `docker compose up -d` 一条命令启动全部服务

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：

- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：

- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示
- 不要保留脚手架默认 README 内容

